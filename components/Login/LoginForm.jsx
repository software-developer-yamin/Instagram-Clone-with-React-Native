import { StyleSheet, Text, View } from "react-native";

const LoginForm = () => {
  return (
    <View>
      <Text>LoginForm</Text>
    </View>
  );
};

export default LoginForm;

const styles = StyleSheet.create({});
